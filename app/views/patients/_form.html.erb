<%= form_with(model: patient, local: true) do |form| %>
  <% if patient.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(patient.errors.count, "error") %> prohibited this patient from being saved:</h2>

      <ul>
      <% patient.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :first_name %>
    <%= form.text_field :first_name, id: :patient_first_name %>
  </div>

    <div class="field">
      <%= form.label :middle_name %>
      <%= form.text_field :middle_name, id: :patient_middle_name %>
    </div>

    <div class="field">
    <%= form.label :last_name %>
    <%= form.text_field :last_name, id: :patient_last_name %>
  </div>

  <div class="field">
    <%= form.label :weight %>
    <%= form.text_field :weight, id: :patient_weight %>
  </div>

  <div class="field">
    <%= form.label :height %>
    <%= form.text_field :height, id: :patient_height %>
  </div>

  <div class="field">
    <%= form.label :mrn %>
    <%= form.text_field :mrn, id: :patient_mrn %>
  </div>

  <div class="actions">
    <%= form.submit "Submit" %>
  </div>

<% end %>
